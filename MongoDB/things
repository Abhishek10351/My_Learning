import pymongo
from pymongo import MongoClient
cluster = pymongo.MongoClient("mongodb+srv://Abhishek:Mongo123@cluster.mso4l.mongodb.net")
#make a connection

db = cluster.test
collection = db.test
# to connect to the db
collection.insert_one({})
collection.insert_many([])
collection.find({})
collection.find_one()
prefixes.update_one({"_id":message.guild.id}, {"$set": {"prefix":new_prefix.strip()}})